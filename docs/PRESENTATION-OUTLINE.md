# Sentinel Gujarat — Solution Presentation outline (≈ 15 slides)

**This deck is generated from this outline.** `node docs/tools/build-presentation.mjs` writes `docs/export/Sentinel-Gujarat-Presentation.pptx` and `docs/export/Sentinel-Gujarat-Presentation.pdf` (17 slides: the 15 below plus an agenda after the title and a "Measured figures" slide before the screenshots); the slide text lives in `buildSlides()` of that script, the **speaker notes are read from the last column of the table below** (matched by slide number), screenshots come from the real-sandbox captures `docs/screenshots/sandbox/*.png` where one exists for the slide (import, map, wall, health — taken 5 Sept 2026 against the 30 organiser cameras), else `docs/screenshots/<nn>-*.png` (moment shots), else `docs/screenshots/live/`, and diagrams from `docs/diagrams/*.svg`. Edit the notes here, the copy in the script, then rebuild; per-slide PNGs land in `docs/export/.build/slides/` for a visual check. 16:9, product name **Sentinel Gujarat**, version `1.0.0-phase1`, footer "Dynatech Consultancy · Gujarat Police Innovation Challenge 2026 · Model 1 + Model 2 (hybrid roadmap to 3/4)". Every screenshot is taken from the hosted URL at 1920×1080, Chrome, logged in as `jury_admin` unless stated; file names below are the ones expected in `docs/screenshots/`.

Order follows plan 6.5 and the portal's presentation requirements (guide §7.2): chosen models with justification → overview, objectives, innovations → architecture + workflow → AI analytics → watchlist correlation and alerting (four-box slide) → technologies → scalability / interoperability / security / deployment → bonus items → operational benefits → screenshots → team.

| # | Slide | Content | Screenshot / figure | Speaker notes (≈ 30–45 s each) |
|---|---|---|---|---|
| 1 | **Title** | Sentinel Gujarat — Unified CCTV registry, viewing and analytics for Gujarat Police. Dynatech Consultancy, Category 2. Hosted URL, repo tag `v1.0-phase1`, date | Logo + `01-login.png` (login page with tagline) | "Sentinel Gujarat is a working platform, hosted at the URL on screen, that registers cameras on a map, watches them live, reads plates, alerts on watchlisted vehicles and traces a vehicle's route. Everything you will see is running software on the organiser's sandbox feeds." |
| 2 | **The problem in Gujarat's terms** | 26 departments, analog + IP, many vendors and VMS, 7–15+ days retention, ~1,000 km, border districts on 4G; police need one platform and a path to 80,000 cameras plus VAHAN/SARTHI/eGujCop/AFIS/NAFIS | Map of Gujarat with the 30 real sandbox cameras across 10 districts and the district boundaries (`sandbox/proof-map.png`; fallback `02-map-districts.png`) | "Every department bought its own cameras. The problem is not more cameras; it is one view over the cameras that exist, without touching what departments already run." |
| 3 | **Chosen models and justification** | Model 1 + Model 2 delivered; hybrid roadmap to Model 3 (adapter framework, event bus) and Model 4 (central recording, K8s, DR). Table: model → delivered / seeded / designed | Diagram: 4 model boxes with status colours | "Every mandatory item lives in Models 1 and 2 and can be shown working on raw RTSP. We seeded Model 3 in code — the adapter interface and webhooks — and designed Model 4 in the Plan for Scale rather than claim it." |
| 4 | **Overview, objectives, innovations** | One-liner; 8 objectives with evidence; innovations: one-connection relay, settings-driven catalogue onboarding with measured time, shared plate normalisation + voting, pre-index + live, operator-in-the-loop routes, evidence hashes, recording next to metadata, open source | `03-dashboard.png` | "Three things we think are different: onboarding a new environment is a settings change, not code; every route is a human-confirmed, hashed evidence chain; and departments only ever see one read-only connection." |
| 5 | **Architecture (HLD 3.1)** | Sources → MediaMTX relay → browsers (WHEP/HLS) and ANPR worker → FastAPI + PostgreSQL/PostGIS → React UI; single VM today, same containers per district at scale | `diagrams/01-architecture.svg` | Walk the diagram left to right; stress "one connection per camera", "worker posts metadata + crops, needs only outbound HTTPS", "PostgreSQL is the only datastore". |
| 6 | **End-to-end workflow** | Onboarding (catalogue / CSV / API / form → validate → upsert → relay path → health → ANPR) and ANPR → sighting → alert; route search | `diagrams/02-onboarding.svg` + `diagrams/03-anpr-alert-sequence.svg` (two halves) | "From catalogue to first stream in seconds — the import summary prints the measured time. Every read is voted for 3 s, normalised the same way in the worker and the API, and matched in-process." |
| 7 | **AI analytics approach** | Decode (PTS-driven, TCP, backoff) → YOLO-v9-t plate detector (ONNX) → PaddleOCR two-line aware → position-aware normalisation (20 test vectors) → char-wise voting → sightings; YOLOX-s vehicle/person counts; quality section with spot-check accuracy; intrusion (P2) and FRS (roadmap) | `04-camera-live-reads.png` (camera page with live reads + object counts) | "Plates are read continuously on the eight road-facing sandbox cameras our survey picked and at keyframe rate on all thirty, so the whole loop is indexed. The organiser footage is night-time, wide-angle, plates 25–110 pixels wide — so we detect on full-resolution tiles and suppress burnt-in text. The quality page prints measured accuracy from operator-labelled crops, not a claim." |
| 8 | **Watchlist correlation & alerting — the four boxes** | **Database structure** (watchlist, plate_reads, sightings, alerts, events, audit) · **Matching logic** (normalise → exact → Levenshtein-1 'possible' at conf ≥ 0.8 → suppression 60 s → priority = max(entry, reason floor) − 1 if possible) · **Alerting mechanism** (WS envelope, desktop notification, webhook HMAC, Telegram P2, escalation > 5 min) · **User interface** (toast, panel, drawer with crop/recording/clip, route) | `diagrams/06-integration-workflow.svg` (full slide) | "The portal asks for the complete integration workflow on one slide — here it is: the tables, the matching rules, the channels and the screens. Priority is derived, not typed: stolen and wanted are always critical." |
| 9 | **Technology stack — all open source** | React, Ant Design, Vite, TanStack Query, Leaflet, hls.js · FastAPI, SQLAlchemy, Pydantic, PostgreSQL + PostGIS · MediaMTX, ffmpeg · PaddleOCR, open-image-models detector, YOLOX, onnxruntime, OpenCV · Caddy, Docker; licence column; AGPL Ultralytics fallback disclosed and off the primary path | Logo grid with licence badges (from `LICENCES.md`) | "No proprietary VMS, SDK or cloud AI anywhere. Licences are listed in the repository; the only AGPL component is a fallback detector that is not used unless the primary fails, and would be disclosed." |
| 10 | **Existing departmental systems remain unaffected** (Model 2 note) | Direct read-only RTSP/ONVIF; exactly one connection per camera via the internal relay; nothing installed at the department; no middleware/federation (FAQ Q17); reversible; failure-isolated | Simple diagram: department NVR → single arrow → relay → all consumers | "This is the Model 2 architecture note. A departmental NVR sees one extra viewer. We write nothing, install nothing, and removing a camera is one click on our side and one credential revoked on theirs." |
| 11 | **Scalability, interoperability, security, deployment** | Edge/district ANPR appliances (2 × L4 ≈ 64 live / 144 keyframe cameras) → Kafka → K8s core → Citus/Timescale + object storage; ≈ 2,800 T4-eq for a mixed 80 k profile ≈ 700 appliances; video stays in the district, metadata ≈ 0.25–0.7 Gbps to centre; storage tiers 7/15/30 d; HA/DR; RBAC in SQL, audit trigger, hashes, TLS, forward-auth | `diagrams/07-scale-topology.svg` + a 4-line cost strip (Topology A ≈ ₹195 Cr capex / ₹46 Cr yr; B ≈ ₹315 / ₹78; B' regional recording ≈ ₹243 / ₹53) | "The same containers run per district. The arithmetic is in the Plan for Scale: decode is the bottleneck, so we size by NVDEC. Our measured figures are CPU-only — eight cameras at 5 fps on 2.7 laptop cores, 2.6 s to onboard the fifty mock cameras and about ninety seconds to onboard the thirty real sandbox cameras including a probe of every stream, 3.3 s read-to-alert — and they sit beside the vendor GPU numbers, which we have not measured ourselves and say so." |
| 12 | **Bonus items delivered** | Cross-camera vehicle tracking with operator confirmation and plausibility flags · recorded evidence + clips with SHA-256 and verify endpoint · privacy controls (retention job, purpose notice, watermark, scoping) · private-society camera onboarding (own feed, `ownership=private`) · integration-ready APIs (OpenAPI, bulk API, webhooks, CSV templates, VAHAN/SARTHI mock adapters) · vehicle/person counting · camera health + maintenance + AMC · desktop notifications | `05-route.png` (route page) small + `06-report-hash.png` (PDF footer with hash) | "None of these replaced a mandatory item; all of them are in the demo. The ones the forensic jurors will care about are the hashes and the confirm/reject trail on every route." |
| 13 | **Operational benefits for policing** | Time-to-route in seconds instead of days; stolen-vehicle alert within seconds of a read with crop and location; one map and one health view for 26 departments; evidence-grade exports; reuse of the installed estate (₹24 k vs ₹1 L+ per camera); zero licence cost; cost-benefit table headline | Bar/tile graphic from `SCALE-PLAN.md` §9 | "For a control room this is: alert, acknowledge, route, PDF — under a minute. For a planner this is: where are the gaps and what is ageing, per department, on one screen." |
| 14 | **Screenshots — the demo journey** | 6-up grid: sandbox import summary with onboarding time · GIS map with layers · 9-tile wall of real sandbox cameras · alert toast + panel · route with timeline · output report PDF | real-camera captures where they exist: `sandbox/proof-map.png`, `sandbox/proof-wall-9.png` (`sandbox/proof-import.png` is a full-page capture — crop it to 16:9 or keep `07-import-summary.png`); the alert / route / report shots (`10-alert-toast.png`, `05-route.png`, `11-output-report.png`) are still from the mock loops until a real read exists | "The map, the wall and the import are the organiser's thirty cameras on this laptop; the alert, route and report screens still come from our rehearsal loops because the night-time sandbox footage has given us one confirmed read so far. Jury credentials for admin, operator and viewer are in the submission form." |
| 15 | **Team, links, Phase 2 readiness** | Dynatech Consultancy team (roles B/M/F/D/P); hosted URL, repo, videos, Drive; Phase 2: settings-driven onboarding of a new environment in < 5 min, live ANPR on all cameras split across VM + laptop, route PDF < 60 s | Team photo / roles table; QR codes for URL and repo | "If shortlisted we bring the stack offline on a GPU laptop, onboard the venue's cameras from Settings, and show the given plate's route live. Thank you." |

## Backup slides (not presented unless asked)

| B1 | Data model (Appendix A of the HLD) | table graphic |
| B2 | Integration-readiness matrix — VAHAN, SARTHI, eGujCop, AFIS, NAFIS | HLD §16 table |
| B3 | Plate normalisation rules and the 20 test vectors | HLD §7.2 |
| B4 | Cost tables in full (Topology A / B / B') | SCALE-PLAN §8 |
| B5 | What is live vs roadmap | HLD §20 |
| B6 | Acceptance checks run before submission (contract §13.3, 30 checks) | checklist graphic |

## Production notes

- The deck is **generated**, not drawn by hand: `cd docs/tools && npm i --no-save && npm run build-presentation` builds the PPTX and its PDF from this outline's slide list and the images in `docs/screenshots/` (`npm run deliverables` also exports HLD, Plan for Scale, REGISTRY-API and LICENCES to PDF with `export-pdf.mjs`; outputs under `docs/export/`). Rebuild after any screenshot or outline change; the same images appear in the HLD and README (consistency item in the submission checklist).
- Screenshot naming: `make screenshots` produces full-page captures named `<nn>_<route>.png`; the moment shots listed above (`01-login.png` … `12-systems-unaffected.png`) are produced by `node docs/tools/moment-shots.mjs` against the running stack (hosted URL via `SG_BASE`), as described in `docs/screenshots/README.md`.
- Diagrams: the ten `.mmd` sources are rendered to `docs/diagrams/<file>.svg` with `npx -y @mermaid-js/mermaid-cli@11 -i docs/diagrams/<file>.mmd -o docs/diagrams/<file>.svg -b white` (all ten verified to render on 4 September; re-render after any edit) and inserted as pictures.
- Keep numbers identical to the HLD and Plan for Scale: camera counts (**30 real sandbox + 1 own**; the 50-camera figures are the mock fixture of the scripted acceptance run and are labelled "mock" wherever they appear), version `1.0.0-phase1`, 8 ANPR-live cameras, cost headline figures.
- Real-sandbox screenshots (`docs/screenshots/sandbox/`, 1440×900, 5 Sept 2026) are listed in `docs/acceptance-log.md` "Real sandbox (5 Sept)"; `proof-import.png`, `ui-import.png`, `proof-health.png` and `ui-settings-catalogue.png` are full-page captures (taller than 16:9) — crop before placing them on a slide.
- Export to PDF with embedded fonts; check that slide 8 (four boxes) is legible at A4.
