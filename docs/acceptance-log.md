# Sentinel Gujarat — acceptance log (contract §13.3, 30 checks)

Filled by the integrator before submission and again after any hot-fix deployed to the hosted URL. One row per check per run; times in IST. "Result" is `pass`, `fail` or `n/a` (with the reason). The measured figures rows at the bottom feed `SCALE-PLAN.md` §1.1 and the HLD §14 cells.

Environment of the run: laptop `CPU=1` — Intel i5-1345U (12 threads), 16 GB RAM, Windows 11 + Docker Desktop (WSL2, 12 vCPU / 7.6 GB), no GPU, `COMPOSE_PROFILES=cpu`, demo profile `ANPR_MAX_CAMERAS=8 ANPR_DETECTOR=contour OBJECT_DETECT=0 ANPR_LIVE_MEMORY=4g` · version `1.0.0-phase1` (working tree of 5 Sept 2026, pre-tag) · run by the integrator (scripted: `e2e.mjs` phases A/B + `frontend/e2e/live_screenshots.mjs`) · date 05 Sep 2026, 00:35–02:10 IST.

| # | Check | Pass condition (short) | Run at (IST) | Result | Evidence / notes |
|---|---|---|---|---|---|
| 1 | Synthetic media | `cam_1…cam_8.mp4` + `plates.json` with the six anchor plates | 01:15 / 01:57 | pass | Generated in the `synth` container in 159 s (118 s on the final fresh run) after the compose entrypoint fix; six anchors on the §12.4 schedule, 24 plates / 69 appearances; files re-encoded **without B-frames** (`has_b_frames=0`) so WebRTC plays them |
| 2 | Stack up | all services healthy; `/healthz` → `status: ok`, `db: ok`, `mediamtx: ok` | 00:37 / 02:00 | pass | Final fresh run (`down -v` → synth → `up -d --build` 27 s → seed): `postgres`, `mediamtx`, `api`, `web` healthy, `anpr-live` + `anpr-preindex` running, `/healthz` = `{"status":"ok","db":"ok","mediamtx":"ok"}` 3 s after up; cold api build 167 s, warm builds api 13 s / web 27 s / anpr 13–16 s / synth 9 s |
| 3 | Login + RBAC basics | `200` + cookie; wrong password `401`; 11th attempt `429` | 00:46 | pass | `access_token` + `Set-Cookie: sg_session … HttpOnly`; wrong password `401 unauthorized`; 11 failed attempts → `401 ×10, 429` |
| 4 | Mock catalogue | 50 items, first item matches §6.1 | 00:46 | pass | 50 items; item 1 = Sachivalaya Gate 1 / Police / 23.2236,72.648 / `rtsp://mediamtx:8554/stream/1` / live |
| 5 | Sandbox import | `fetched=50, added=50, anpr_enabled=8, relay_paths_created=61`, `first_stream_ready_ms < 15000`; rerun `unchanged=50` | 00:46 / 02:00 | pass | Fresh run: `duration_ms` 2566 with `first_stream_ready_ms` 2066 (first run 3857 / ≈ 2300 after the `cookieCheck` redirect fix — previously null), `relay_paths_created` 61, `anpr_enabled` 8, errors 0; rerun `added 0 / updated 0 / unchanged 50` in 14–44 ms; dept_admin → 403 |
| 6 | Registry + geo | `total=50`; 50 GeoJSON features; 8 live; departments mapped, zero UNASSIGNED | 00:46 | pass | `/cameras?source=sandbox` total 50; `/geo/cameras` 50 features, 8 `live`; POLICE/HEALTH/GSRTC/PANCHAYAT/MUNICIPAL, no UNASSIGNED; `/geo/districts` 33; new `GET /api/departments` → 27 |
| 7 | MediaMTX paths | ≥ 70 configured paths | 00:46 | pass | 71 configured paths: 8 `stream/*`, `own_gate`, `all_others`, 50 `cam_*`, 11 `cam_*_h264` |
| 8 | Streams API | H.265 camera → `_h264` play path; audit `stream.view` | 00:46 | pass | mock 8: `codec H265`, `play_path cam_8_h264`, `whep_url /mtx/cam_8_h264/whep`; mock 1: `cam_1`; `stream.view` rows in the audit log |
| 9 | Wall in browser | 4 tiles WebRTC ≤ 5 s; HLS switch ≤ 10 s; 16-grid snapshots; H.265 via `_h264` | 01:37 | pass | Chrome (Playwright): 4-grid all four tiles `LIVE H.264 · WebRTC` at 1280×720 within 5 s; 9-grid all nine WebRTC incl. `H.265→H.264 · WebRTC` (camera 8); 16-grid 4 live + snapshot tiles refreshing each second; HLS path verified through `/mtx` (playlist + `.ts` segment 200, 401 without auth) and by the automatic HLS fallback observed with the earlier B-frame files |
| 10 | Health poller | `online ≥ 8`, `offline == 42` after ≤ 4 polls; `stats` every 10 s | 00:49 | pass | 121 s after import: online 9 (8 live + own gate), offline 42, unknown 9 (CSV/bulk rows without RTSP); `/ws/health` `stats` every 10 s; first-tick `degraded` flip after API restarts fixed (previous counter = 0) |
| 11 | Offline transition | offline + low-priority alert within 4 polls; path re-created; auto-close "Camera back online" | 01:17 | pass | Exercised by stopping MediaMTX for 3 min: 9 cameras offline after 3 ticks with `camera_offline` (`low`) alerts, `camera_offline`/`camera_online` events, all alerts auto-closed `resolved` / "Camera back online" on the first tick after restart; the API re-added the 61 lost runtime paths |
| 12 | ANPR reads | valid reads present; `GJ 01 AB 1234` exact hits; `cameras_seen ≥ 3` after 10 min; `loop_reset` events | 00:51 | pass | Reads within 20 s of worker start; `GJ 01 AB 1234` exact on 5 cameras (1, 3, 6, 2 + own gate); the seamless `-stream_loop` mock produces no PTS discontinuity, so `loop_reset` was verified by kicking the worker's RTSP session on `cam_3`: decoder restart + `loop_reset` event 5 s later (after the RTSP-EOF reconnect fix) |
| 13 | Seeded watchlist alert | `watchlist_hit`, `critical`, `exact`, `latency_ms < 5000`; crop 200 with token / 401 without | 00:52 | pass | `critical` / `exact`, `latency_ms` 2926–3615, snapshot present; crop 200 with `X-Sentinel-Sha256`, 401 without token |
| 14 | Live watchlist add → alert | `GJ27XY3456` alert within 90 s, `latency_ms ≤ 5000`; second read → `alert_update` `read_count=2` | 00:52 | pass | WS `alert` 15 s after the operator added `GJ 27 XY 3456`; `latency_ms` 2760; `notify_title` "HIGH · Suspect GJ 27 XY 3456"; next loop → `alert_update` `read_count` 2 (suppression) |
| 15 | Ack / close / RBAC | operator `200`; viewer `403`; audit `alert.ack` actor `jury_operator` | 00:53 | pass | viewer 403; operator ack 200 `acknowledged_by_username jury_operator`; second ack 409; `alert.ack` audit row with IP |
| 16 | Route | ≥ 3 sightings in order; polyline length = sightings; legs flagged `implausible_speed` | 00:53 | pass | 6 stops / 5 cameras ordered by `first_seen`, `polyline.length == stops`, 56 km total; every leg with distance > 0 flagged (a repeat of the same camera 90 s later is a 0 km leg and is not flagged — correct) |
| 17 | Confirm + route PDF | `{"saved":1}`; PDF opens; `X-Sentinel-Sha256` = `sha256sum` | 00:53 | pass | `{"saved":1}`; `route_GJ01AB1234_20260905_0052IST.pdf` 72–243 KB, `%PDF` magic, header hash == SHA-256 of the body; viewer 403 |
| 18 | Output report | CSV rows = API total; IST first column; PDF with quality + counts; audit ×2 | 00:53 | pass | CSV 68 (later 2020) data rows == `/detections` total, first column `05 Sep 2026, 00:50:53 IST`, `#` trailer with row hash, header hash == body; PDF 465–799 KB with quality section; `report.detections` audited |
| 19 | CSV import | `added=8`; errors rows 4 and 9 (three entries); warning row 7; rerun `updated=8` | 00:47 | pass | `rows_total 10, added 8`; errors rows 4 (`lat`) and 9 (`external_id`, `name`); warning row 7 `department_code`; error CSV header + 3 lines (401 without token); rerun `updated 8` |
| 20 | Bulk API | `added=1`; internal key `403`; no key `401`; `/api/docs` lists it | 00:47 | pass | `GSRTC-101` added (id 59); internal key 403; no key 401; `/api/docs` 200, `openapi.json` has `/api/v1/cameras/bulk` (81 paths) |
| 21 | Department scoping | Police rows only; other camera `404`; scoped `/ws/alerts` | 00:47 | pass | `dept_admin_police`: 21 rows, all `POLICE`; mock 3 (HEALTH) → 404; export CSV contains no HEALTH rows |
| 22 | Gap analysis | `uncovered_pois ≥ 3` (Gandhinagar); zero-coverage cells; ageing includes CSV-006; exports with hashes | 00:47 | pass | 8 uncovered POIs in Gandhinagar (17 total); 237 249 zero-coverage cells (5 000 returned, truncated flag); ageing includes Okha Checkpost; 15 department gaps; recommendations per district; CSV + PDF with `X-Sentinel-Sha256`; 5.5–8.8 s uncached |
| 23 | Recordings + clip + verify | segments ≥ 1; clip `201` with hash; verify `match=true`; corrupted → `match=false` | 01:45 | pass | 16 segments for camera 1; clip 201 with `sha256` + `size_bytes`; `/evidence/verify` match true (clip and crop); `/media/clips/…` range request 206; playback MP4 1.4 MB through `/playback` with the session cookie; clip at the exact segment boundary fixed (ms-precision RFC 3339 + clamp); corruption → `match=false` verified by the backend builder's `phase1_api.py` (127/127) and re-run in the final fresh-DB pass |
| 24 | Settings + catalogue test | bad URL → `ok:false` and `502`; restore works; password masked | 01:46 | pass | `catalogue.base_url=http://api:8000/nowhere` → test `ok:false`, import `502 upstream_error` naming the URL; restore → test ok (50), import `unchanged 50`; unknown key 422; viewer 403; `catalogue.auth_password` masked |
| 25 | Webhook | delivery in the mock sink with valid signature; `last_status=200` | 01:46 | pass | webhook created (secret masked), test delivery 204; next `alert.created` in the sink after 65 s with `X-Sentinel-Signature: sha256=…`; `last_status` 204; deleted afterwards |
| 26 | Retention (dry run) | counts printed, no audit row; `--run` writes `retention.purge` | 01:05 | pass | `python -m app.jobs.retention --dry-run` prints per-category counts (all 0 on fresh data, `dry_run: true`); `--run` executed in the final pass (deletes nothing, writes `retention.purge`) |
| 27 | Audit immutability | `DELETE` fails "audit_log is append-only"; operator `403` on `/audit` | 01:05 | pass | `DELETE` and `UPDATE` on `audit_log` → `ERROR: audit_log is append-only` (trigger); `/api/audit` as operator → 403; login rows carry IP + user agent |
| 28 | Browser UX | viewer has no mutating buttons; IST everywhere; toast + sound + desktop notification; MOCK SANDBOX badge; `/about` version | 01:37 | pass | 45 live screenshots in `docs/screenshots/live/`: viewer pages without Add/Import/Export/Ack, `/audit` → Access denied; all times `dd MMM yyyy, HH:mm:ss IST`; toast for the plate added live (14 s); `MOCK SANDBOX` badge; `/about` shows `1.0.0-phase1` + `/api/docs`; sound and OS notification not observable in headless Chrome (permission banner + test button present) |
| 29 | Tests | pytest green (normalise, route, rbac, csv_import, matcher); `npm run typecheck && npm run build` clean, `dist/` < 6 MB | 01:55 | pass | backend `pytest -q` in the api image and `anpr/tests` in the anpr image green (see chain log); `tsc --noEmit`, `eslint --max-warnings=0`, `vite build` clean, `dist/` 4.0 MB |
| 30 | GPU profile validates | `config` renders with NVIDIA reservation and `MEDIAMTX_TRANSCODE=nvenc` (documented as untestable on the laptop) | 01:05 | pass | `COMPOSE_PROFILES=gpu … -f deploy/docker-compose.gpu.yml config` renders: `driver: nvidia` reservations on both GPU workers and MediaMTX, `MEDIAMTX_TRANSCODE: nvenc`, `deploy/mediamtx-nvidia.Dockerfile` |

## Measured figures captured during the run (copy into `SCALE-PLAN.md` §1.1)

| Figure | Source | Value |
|---|---|---|
| Onboarding time for 50 cameras (`duration_ms`) | check 5 import summary | 2 566 ms on the fresh clone (fetch + upsert + 61 relay paths + first-stream wait); the first run before the `cookieCheck` redirect fix took 3 857 ms; rerun with 50 unchanged 14–44 ms |
| First stream ready (`first_stream_ready_ms`) | check 5 | 2 066 ms cold on-demand pull (HLS manifest) on the fresh clone (≈ 2 300 ms on the first run); 44 ms warm |
| Reads/s sustained by the API | `reads.last_1h / 3600` from `/dashboard/stats` during the soak | content-bound on the mock: 0.3–0.6 reads/s from 8 loops; API cost 15–40 ms per detection batch, 12–20 ms per snapshot at ≈ 16 internal requests/s and 8–14 % of one core |
| Read → alert latency p50 / p95 (`alerts.latency_ms`) | checks 13–14 + 24 h soak | steady state p50 3.3 s / p95 4.0 s / max 4.1 s (n = 13); whole run p50 3.4 s / p95 6.2 s with 11–22 s outliers only during an API restart and the headless-Chrome memory-pressure freezes |
| Cameras per worker at achieved fps (heartbeat `fps_actual`) | `/health` ANPR workers | 8 cameras at 5.0 fps decode each; 19–21 frames/s inferred; 2.7 cores; 2.0–2.3 GB RSS |
| DB size per 1,000 sightings | `pg_total_relation_size` on `sightings` + `plate_reads` | 2.5 MB (794 KB for 314 sightings + 315 reads) |
| Crop + frame bytes per sighting | `du /data/crops /data/frames` ÷ sightings | 7.1 KB crop + 30.7 KB frame |
| Recording GB per camera-hour | `recordings_used_bytes` ÷ camera-hours | ≈ 0.12 GB per camera-hour (720p synthetic loops at ~300 kbps) |

## Hot-fixes deployed after tagging `v1.0-phase1`

| Date/time (IST) | Tag | Change | Checks re-run |
|---|---|---|---|
| | | | |
