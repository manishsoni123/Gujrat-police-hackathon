# Sentinel Gujarat ANPR worker - pinned runtime dependencies (CPU and GPU images).
# The ONNX runtime wheel is chosen by the Dockerfile from the ANPR_BASE build arg:
#   cpu -> onnxruntime==1.19.2        gpu -> onnxruntime-gpu==1.19.2
# All packages below are Apache-2.0 / BSD / MIT licensed (see anpr/README.md).
numpy==1.26.4
opencv-python-headless==4.10.0.84
pillow==10.4.0
pyyaml==6.0.2
requests==2.32.3
# PaddleOCR PP-OCRv4 (Apache-2.0) on the CPU PaddlePaddle wheel (Apache-2.0)
paddlepaddle==2.6.2
paddleocr==2.8.1
# paddleocr transitive dependencies, pinned so the image is reproducible
shapely==2.0.6
scikit-image==0.24.0
imgaug==0.4.0
pyclipper==1.3.0.post5
lmdb==1.5.1
tqdm==4.66.5
rapidfuzz==3.10.0
cython==3.0.11
python-docx==1.1.2
beautifulsoup4==4.12.3
fonttools==4.54.1
fire==0.7.0
scipy==1.14.1
imageio==2.35.1
matplotlib==3.9.2
protobuf==4.25.5
# tests
pytest==8.3.3
# fast-plate-ocr plate recogniser (MIT; anpr/ocr.py FastPlateOCR). Its numpy / opencv-headless / pyyaml / tqdm are the
# pins above; rich (+ markdown-it-py, mdurl, pygments) is what it imports at run time
fast-plate-ocr==1.1.0
rich==13.9.4
markdown-it-py==3.0.0
mdurl==0.1.2
pygments==2.18.0
